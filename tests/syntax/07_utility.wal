;; ============================================================
;; 07_utility.wal — Test exit (eval-file is runtime)
;; ============================================================

;; exit — (syntax only, won't actually execute)
;; (exit 0)
;; (exit 1)

;; This file validates that exit is a known symbol
;; (syntax-check only, not for actual execution)
(define status 0)
;; (exit status)  — commented out to not actually exit
